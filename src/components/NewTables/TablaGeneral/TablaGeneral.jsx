import React, { useEffect } from 'react';
import { useState, useSelector } from 'react-redux';
import axios from 'axios';

export default function TablaGeneral(props) {
  const { titulo, subtitulo, columnas, url, filtroSort, numPaginado, acciones } = props;

  const getData = () => {
    
  }

  return (

    <>

    </>

  );
}